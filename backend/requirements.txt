Django==5.0.6
djangorestframework==3.17.1
# django-fsm-2 is the maintained django-commons fork of django-fsm, which was
# abandoned at 3.0.1 (that version's import-time UserWarning points at
# viewflow.fsm, an AGPL-licensed rewrite with a different API and no
# FSMModelMixin -- see the FSM state machines note in the root README).
# Same `django_fsm` module name and same public API, so imports, FSMField
# deconstruct paths in existing migrations, and MIT licensing all carry over.
django-fsm-2==4.2.4
django-simple-history==3.12.0
psycopg2-binary==2.9.12
celery==5.4.0
redis==5.0.4
django-auth-adfs==1.16.0
# Already present transitively via django-auth-adfs, but pinned explicitly
# because apps/notifications/graph.py imports it directly: an indirect
# dependency that a future django-auth-adfs release stops requiring would
# take the mail transport with it.
requests==2.33.1
python-dotenv==1.0.1
pyotp==2.9.0
boto3==1.34.144

# Report PDF pipeline (apps/reporting/tasks.py, Blueprint Section 2.1a).
# WeasyPrint links against system libraries at import time (Pango, cairo,
# GDK-PixBuf). On Debian/Ubuntu those are libpango-1.0-0, libpangoft2-1.0-0,
# libgdk-pixbuf-2.0-0 and libcairo2, which CI installs explicitly (see
# .github/workflows/ci.yml). A Windows dev box needs the GTK runtime --
# see the README's Running it yourself section.
weasyprint==69.0
jinja2==3.1.4

# Production serving. `manage.py runserver` is a development server and
# Django says so explicitly; it is single-threaded, does not handle
# concurrency, and has never been security-audited for public exposure.
# gunicorn is what the container's CMD runs.
gunicorn==23.0.0
# Static files for the Django admin and DRF's browsable API, served by the
# app itself rather than by a separate nginx. Both SPAs are built and
# deployed separately, so this is a small surface -- but without it the
# admin renders unstyled, which looks exactly like a broken deployment.
whitenoise==6.7.0
